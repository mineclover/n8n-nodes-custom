{
  "name": "n8n-nodes-custom",
  "version": "0.1.0",
  "private": true,
  "description": "Custom n8n nodes monorepo",
  "author": "junwoobang",
  "license": "MIT",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r --parallel dev:watch",
    "dev:link": "pnpm -r --parallel dev:link",
    "lint": "pnpm -r lint",
    "clean": "pnpm -r exec rm -rf dist node_modules"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}
